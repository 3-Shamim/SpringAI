spring:
  application:
    name: advisor
  profiles:
    active: dev
  ai:
    openai:
      api-key:
      chat:
        model: gpt-4o

logging:
  level:
    root: info
    org.springframework.ai.chat.client.advisor.SimpleLoggerAdvisor: debug
