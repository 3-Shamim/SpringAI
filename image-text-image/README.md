### Image to Text to Image
* Image to text:
  * Which is working file
* Text to image:
  * `dall-e-*` models are no longer present in OpenAI
  * Now they provide `gpt-image-*` models
    * Only supports `b64_json`
    * `URL` doesn't support